import React from 'react';
import { Typography, Box } from '@mui/material';

const ChildrenReports: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4" gutterBottom>Отчёты по детям</Typography>
    <Typography>Здесь появятся отчёты по детям и их посещаемости. (В разработке)</Typography>
  </Box>
);

export default ChildrenReports;
