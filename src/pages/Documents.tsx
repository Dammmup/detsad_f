import React from 'react';
import { Typography, Box } from '@mui/material';

const Documents: React.FC = () => (
  <Box p={3}>
    <Typography variant="h4" gutterBottom>Все документы</Typography>
    <Typography>Здесь будет реализован общий раздел для всех документов. (В разработке)</Typography>
  </Box>
);

export default Documents;
